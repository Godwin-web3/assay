# Solana — live source of truth for mint and wallet reads
SOLANA_RPC=https://api.mainnet-beta.solana.com

# Supabase — durable book (instruments + snapshots)
# Server writes use the service role. Do not expose it to the browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
